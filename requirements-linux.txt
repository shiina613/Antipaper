# Linux baseline requirements for Antipaper / Paperless Meetings.
#
# Target:
# - Python 3.10-3.12
# - Ubuntu/Debian or similar Linux distro
# - CPU-first install that runs Streamlit, PyMuPDF, YOLOv8, DOCX ingestion,
#   and the current document-grounded chatbot.
#
# Optional OCR/Paddle GPU dependencies are intentionally not pinned here because
# they depend on the machine CUDA version. See notes at the bottom.

fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
streamlit>=1.25.0

PyMuPDF>=1.23.0
pdf2image>=1.16.3
opencv-python-headless>=4.8.0
Pillow>=10.0.0
python-docx>=1.1.0

ultralytics>=8.0.0
huggingface_hub>=0.20.0

langchain>=0.1.0
python-dotenv>=1.0.0
httpx>=0.25.0

# Testing/dev utilities used by the current repo tests.
pytest>=8.0.0

# Optional OCR fallback on Linux CPU:
# Uncomment only if the teammate needs PaddleOCR fallback.
# paddlepaddle==3.2.2
# paddleocr==3.4.1
# paddlex[ocr]==3.4.3
#
# Optional OCR fallback on Linux GPU:
# Install PaddlePaddle GPU from the official Paddle instructions matching the
# host CUDA version, then install:
# paddleocr==3.4.1
# paddlex[ocr]==3.4.3
#
# Do not reuse the Windows-only paddlepaddle-gpu wheel URLs from requirements.txt.
